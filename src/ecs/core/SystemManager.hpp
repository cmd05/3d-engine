#pragma once

#include <cassert>
#include <memory>
#include <unordered_map>
#include <typeindex>

#include "System.hpp"

class SystemManager {
public:
    template<typename T>
    T& register_system() {
        
    }
}